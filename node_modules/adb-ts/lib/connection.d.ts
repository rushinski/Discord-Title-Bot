/// <reference types="node" />
import { Socket, SocketConstructorOpts } from 'net';
import { Parser } from './parser';
export declare class Connection extends Socket {
    readonly parser: Parser;
    constructor(options?: SocketConstructorOpts);
}
