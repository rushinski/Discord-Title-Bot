/// <reference types="node" />
import EventEmitter from 'events';
declare const autoUnregister: <T extends EventEmitter, R = void>(emitter: T | Promise<T>, action: Promise<R> | ((emitter: T) => Promise<R>)) => Promise<R>;
export default autoUnregister;
