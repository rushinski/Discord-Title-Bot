import { Connection } from '../../connection';
import { ArgsMapper } from '../../util';
import ExecCommand from './exec';
export default abstract class FileSystemCommand<T extends object, P extends string | string[]> extends ExecCommand<void> {
    protected abstract rootCmd: string;
    protected abstract argsMapper: ArgsMapper<T>;
    private path;
    private options;
    constructor(connection: Connection, serial: string, path: P, options?: T);
    protected get rawCmd(): string;
    protected cast(): void;
}
