import TransportCommand from './transport';
export default abstract class TransportParseValueCommand<T> extends TransportCommand<T> {
    protected keepAlive: boolean;
    protected abstract parse(value: string): T;
    protected postExecute(): Promise<T>;
}
