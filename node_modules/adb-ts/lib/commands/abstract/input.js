"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const util_1 = require("../../util");
const transport_1 = __importDefault(require("./transport"));
class Input extends transport_1.default {
    constructor(connection, serial, source, command, args, withEscape = false) {
        super(connection, serial);
        this.keepAlive = false;
        this.Cmd = ['shell:input', source]
            .concat(command, [args]
            .flat()
            .filter((a) => typeof a !== 'undefined' && a !== '')
            .map((a) => (withEscape ? (0, util_1.escape)(a) : String(a))))
            .join(' ');
    }
    postExecute() {
        return;
    }
}
exports.default = Input;
