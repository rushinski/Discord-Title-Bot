import TransportParseAllCommand from '../abstract/transportParseAll';
export default class ListPackagesCommand extends TransportParseAllCommand<string[]> {
    protected Cmd: string;
    protected parse(value: string): string[];
}
