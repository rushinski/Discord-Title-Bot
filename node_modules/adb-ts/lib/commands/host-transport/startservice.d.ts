import { Connection } from '../../connection';
import { StartServiceOptions } from '../../util';
import StartProcess from '../abstract/startProcess';
export default class StartServiceCommand extends StartProcess {
    protected keepAlive: boolean;
    protected internalCmd: string;
    constructor(connection: Connection, serial: string, pkg: string, service: string, options?: StartServiceOptions);
}
