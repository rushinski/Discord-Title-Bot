import { MonkeyCallback } from '../util';
export declare abstract class BaseCommand<T> {
    readonly command: string;
    readonly callback: MonkeyCallback<T>;
    constructor(command: string, callback: MonkeyCallback<T>);
    abstract isParsable(): this is ParsableCommand<T>;
}
export declare class Command extends BaseCommand<null> {
    isParsable(): this is ParsableCommand<null>;
}
export declare class ParsableCommand<T> extends BaseCommand<T> {
    isParsable(): this is ParsableCommand<T>;
    readonly parser: (data: string | null) => T;
    constructor(command: string, callback: MonkeyCallback<T>, parser: (data: string | null) => T);
}
